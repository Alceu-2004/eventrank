# .env.example - exemplo de variáveis de ambiente para o backend EventRank

# Host do banco de dados (geralmente localhost para desenvolvimento local)
DB_HOST=localhost

# Porta do banco de dados (PostgreSQL padrão: 5432)
DB_PORT=5432

# Nome do banco de dados que será usado pelo projeto
DB_NAME=eventrank

# Usuário do banco de dados 
DB_USER=seu_usuario

# Senha do banco de dados 
DB_PASS=sua_senha

# OBS: Para rodar localmente, copie este arquivo para .env e preencha DB_USER e DB_PASS reais